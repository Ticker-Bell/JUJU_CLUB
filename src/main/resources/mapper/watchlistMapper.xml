<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tickerbell.jujuclub.stock.mapper.WatchlistMapper">

  <!-- WatchlistMapper(관심종목) : 목록 조회 / currentPrice, changePct : 서비스에서 값 계산 -->
  <select id = "selectWatchlistItems" parameterType="int" resultType="WatchlistItemDTO">
    select uw.favorite_seq as favoriteSeq, uw.stock_seq as stockSeq, sm.stock_code as stockCode, sm.stock_name as stockName
    from user_watchlist uw
    join stock_master sm on uw.stock_seq = sm.stock_seq
    where uw.user_seq = #{user_seq}
    order by uw.favorite_seq DESC
  </select>

</mapper>